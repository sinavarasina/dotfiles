return {
    c      = require("lua.core.constants"),
    path   = require("lua.core.paths"),
    app    = require("lua.core.apps"),
    shell  = require("lua.core.shell"),
    notify = require("lua.core.notify"),
    key    = require("lua.core.key"),
    match  = require("lua.core.match"),
    env    = require("lua.core.env"),
    module = require("lua.core.module"),
}
